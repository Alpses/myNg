import { HighttextDirective } from './highttext.directive';

describe('HighttextDirective', () => {
  it('should create an instance', () => {
    const directive = new HighttextDirective();
    expect(directive).toBeTruthy();
  });
});
